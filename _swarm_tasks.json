{
  "tasks": [
    {
      "context": "Project: C:\\Aegentix. .hermes.md governs. Model: openai/gpt-6-astra via Nous inference API. Security boundaries: Falco rules in security/falco-aegentix-rules.yaml are ground truth. agent_security_engine.py provides behavioral anomaly detection (5 indicators). Port whitelist: 80, 443, 9090, 3001, 9093, 8080, 7070-7073. Secrets in .env only. docker-compose.security.yml references both files but they DON'T EXIST yet. docker-compose.autonomous.yml references 5 Dockerfiles in agents/infrastructure/ that DON'T EXIST yet (Dockerfile.monitoring, Dockerfile.incident, Dockerfile.autoscaling, Dockerfile.healing, Dockerfile.deployment). The config YAMLs in agents/infrastructure/config/ are empty stubs. Write ONCE, verify with cat after.",
      "goal": "Create the two missing security ground-truth files that docker-compose.security.yml references. 1) security/falco-aegentix-rules.yaml — Falco rules for container-level security events on the Aegentix stack. Cover: shell spawning in containers, sensitive mount access, reverse shell patterns, unexpected network egress to non-whitelisted ports, privileged container escape attempts, /etc/shadow or key material access, process injection/signals to other containers. Use falco rule schema (rule, desc, condition, output, priority, tags). Keep priority levels: critical, high, medium, low. 2) security/agent_security_engine.py — Python behavioral anomaly detector with exactly 5 indicators: (1) heartbeat gaps, (2) decision-loop velocity anomalies, (3) integrity score velocity drops, (4) action/output size anomalies, (5) cross-agent communication pattern deviations. Expose as a class SecurityEngine with .record_event(agent, event_type, payload) and .check_anomalies() -> list[dict]. Write to C:\\Aegentix\\security\\agent_security_engine.py and C:\\Aegentix\\security\\falco-aegentix-rules.yaml. Create security/ dir if missing.",
      "output_schema": {}
    },
    {
      "context": "Project: C:\\Aegentix. docker-compose.autonomous.yml defines 5 infra agent services (agent-monitoring, agent-incident-response, agent-autoscaling, agent-healing, agent-deployment), each building from agents/infrastructure/ with a role-specific Dockerfile (Dockerfile.monitoring, Dockerfile.incident, Dockerfile.autoscaling, Dockerfile.healing, Dockerfile.deployment). The config YAMLs in agents/infrastructure/config/ are empty stubs (0 bytes) — populate them. The agent entrypoint Python files DON'T EXIST yet. .hermes.md: integrity scores per-agent, Guardian approves security/port/process/egress changes, all actions require evidence + verification. Write ONCE, verify with cat after.",
      "goal": "Build out the 5 infrastructure agents that docker-compose.autonomous.yml expects. For EACH agent role (monitoring, incident-response, autoscaling, healing, deployment): (a) write agents/infrastructure/<role>.py — e.g. agents/infrastructure/monitoring.py, agents/infrastructure/incident_response.py, agents/infrastructure/autoscaling.py, agents/infrastructure/healing.py, agents/infrastructure/deployment.py. Each is an AutonomousAgent subclass (import from autonomous_swarm.AutonomousAgent or copy the class) with a run() loop doing role work, recording events to a local JSONL log at orchestrator/events/<role>.jsonl, and exposing a /metrics HTTP endpoint on port 8080 (use http.server) serving Prometheus text format with at least: aegentix_<role>_operations_total, aegentix_<role>_operations_duration_seconds, aegentix_<role>_integrity_score, up. (b) write agents/infrastructure/Dockerfile.<variant> where variant matches the compose service name exactly: Dockerfile.monitoring, Dockerfile.incident, Dockerfile.autoscaling, Dockerfile.healing, Dockerfile.deployment — Python slim base, copies the role .py, runs it. (c) populate the empty config YAMLs: agents/infrastructure/config/monitoring.yml (scrape intervals, alert thresholds, grafana dashboard refs), incident-response.yml (severity escalation matrix, Slack webhook placeholder env, runbook URLs), autoscaling.yml (scale thresholds CPU/mem, min/max instances, cooldown), healing.yml (restart policy, max retries, health check intervals, drain behavior), deployment.yml (canary config, rollback triggers, registry placeholder). All configs must be valid YAML with real content, not stubs.",
      "output_schema": {}
    },
    {
      "context": "Project: C:\\Aegentix. .hermes.md section 'Orchestration Primitives' says Kanban is a durable work queue replacing empty orchestrator/ dirs. Columns: Inbox -> Swarm Consensus -> Execution -> Verification -> Security -> Observability -> Done. .hermes.md also says orchestrator/workflows/workflow-*.json is legacy being migrated to Kanban, and orchestrator/events/events.log is legacy being migrated to Hermes session transcripts. Hermes delegates should read swarm_state.json and active_swarm_manifest.json before swarm-level decisions. Write ONCE, verify with cat after.",
      "goal": "Build the Kanban work queue and event log migration layer. 1) Create orchestrator/kanban/state.json initialized with columns: inbox, swarm_consensus, execution, verification, security, observability, done — each an empty list. Add a 'meta' key with version '1.0-hermes' and swappable columns list. 2) Create orchestrator/kanban/kanban.py with a KanbanBoard class: .add_card(column, card_dict) -> str (id), .move_card(card_id, to_column) -> bool, .get_cards(column) -> list, .get_all() -> dict, persisting to state.json on every mutation (atomic write with tmpfile+rename). Cards have: id, title, description, column, owner (agent name from the 5-agent swarm or infra agent roles), evidence (path or null), verification (bool), created_at, moved_at. 3) Create orchestrator/events/ dir and an events_logger.py with EventsLogger class: .log(event_dict) appends to orchestrator/events/events.jsonl (one JSON object per line, with timestamp, source_agent, event_type, payload). Provide a migration note in a docstring that events.log format is legacy and jsonl is the Hermes-native format. 4) Seed the inbox with 3 representative cards that a swarm would actually triage: 'Falco rules ground-truth missing', 'Infra agent Dockerfiles missing', 'Observability stack not running' — each tagged for the right owner column=inbox.",
      "output_schema": {}
    },
    {
      "context": "Project: C:\\Aegentix. Three docker-compose files exist: docker-compose.yml (7 services: runtime, gaia, directive, hud, mcp-fs, mcp-http, mcp-system), docker-compose.autonomous.yml (5 infra agents + full monitoring/observability stack: prometheus, grafana, alertmanager, elasticsearch, kibana, filebeat), docker-compose.security.yml (hardened core + Falco + telemetry engine, separate aegentix-internal network). .hermes.md says GAIA on 8090 is 'not yet running'. Port whitelist: 80, 443, 9090, 3001, 9093, 8080, 7070-7073. Write ONCE, verify with cat after.",
      "goal": "Create a single unified docker-compose.hermes.yml that layers ALL services together for a full Aegentix stack bring-up via Hermes orchestration, respecting the port whitelist and the security model from .hermes.md. The unified compose must: (a) include all 7 runtime services from docker-compose.yml (runtime:8080, gaia:8090, directive:7070, hud:3000, mcp-fs:7071, mcp-http:7072, mcp-system:7073) — note hud maps 3000->3000 NOT 3001 (3001 is grafana's external port in autonomous.yml, so hud stays on 3000); (b) include the 5 infra agents from docker-compose.autonomous.yml (agent-monitoring, agent-incident-response, agent-autoscaling, agent-healing, agent-deployment) — but they need the Dockerfiles from the infra-agent delegate first; for now write the compose service entries with build contexts and a TODO comment in each Dockerfile line noting the file must exist; (c) include the monitoring/observability stack from autonomous.yml (prometheus:9090, grafana:3001, alertmanager:9093, elasticsearch, kibana, filebeat) — but SKIP elasticsearch and kibana (they're heavy and not on the port whitelist; filebeat can ship to a remote ES or we add a lightweight alternative later — comment them out with rationale); (d) include the security services from docker-compose.security.yml (aegentix-falco-monitor on aegentix-internal network, aegentix-telemetry-engine running agent_security_engine.py) — but SKIP the aegentix-core hardcoded image (it doesn't exist as a built image; the runtime service already covers port 8080); (e) use ONE shared network aegentix-net for everything that needs to talk (infra agents, monitoring, runtime services), and a separate internal network aegentix-internal for Falco+telemetry (no published ports, matching security.yml). Publish ONLY whitelisted ports. Add a top-level comment block explaining the layering.",
      "output_schema": {}
    },
    {
      "context": "Project: C:\\Aegentix. .hermes.md is the orchestration policy. It says: Hermes is the orchestration brain, coordinates the 5 swarm agents (Visionary, Guardian, Builder, Wise, Executor from autonomous_swarm.py) via delegate_task, cronjob, and Kanban. Guardian must approve security/port/process/egress changes. Swarm consensus via SwarmManager.swarm_think() is authority for cross-agent decisions. Hermes delegates read swarm_state.json and active_swarm_manifest.json before swarm-level decisions. The swarm_state.json and active_swarm_manifest.json already exist. Hermes delegates have terminal, file, code_execution, delegation tools. Model: openai/gpt-6-astra via Nous inference API. Write ONCE, verify with cat after.",
      "goal": "Write the Hermes-native glue that connects Hermes delegates to the existing Python swarm. Create two files: 1) swarm_adapter.py — a Python module that, given a situation dict, loads swarm_state.json + active_swarm_manifest.json, imports SwarmManager from autonomous_swarm, calls swarm_think(situation), and returns the consensus + per-agent decisions as a structured dict suitable for a Hermes delegate to act on. Include a __main__ block that takes a JSON situation from stdin or a --situation-file and prints the result as JSON to stdout (so a Hermes delegate can shell out to it). 2) orchestrator/policy/hermes-swarm-policy.txt — a plain-text policy file that states, in <= 400 words, the decision authority matrix: which agent (Visionary/Guardian/Builder/Wise/Executor) owns which decision domain, when Guardian approval is required, when swarm_consensus is required vs single-agent authority, and the integrity-score floor (50) rule. Reference the port whitelist and security boundaries from .hermes.md. This file is the human-readable counterpart to the code.",
      "output_schema": {}
    }
  ]
}
